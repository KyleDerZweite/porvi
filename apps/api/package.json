{
  "name": "@porvi/api",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@porvi/platform-auth": "workspace:*",
    "@porvi/platform-contracts": "workspace:*",
    "@porvi/platform-core": "workspace:*",
    "@porvi/platform-db": "workspace:*",
    "@hono/node-server": "^1.14.1",
    "@hono/zod-validator": "^0.4.3",
    "hono": "^4.7.7"
  },
  "devDependencies": {
    "tsx": "^4.19.4"
  }
}
